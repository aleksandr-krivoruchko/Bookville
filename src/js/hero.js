import { refs } from './common-ref';

refs.heroBtn.addEventListener('click', () => console.log('go to ...'));
refs.heroArrowLeft.addEventListener('click', () => console.log('left'));
refs.heroArrowRight.addEventListener('click', () => console.log('right'));
